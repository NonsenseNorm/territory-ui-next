import { ScrollView,Text,View,Pressable,StyleSheet } from 'react-native';
import { scenario } from './model';
const entries=[['signed-out','/login','初回起動・ログイン'],['signed-out','/register','新規登録から始める'],['offline','/','ログイン済み・NFC未参加'],['online','/','場所に参加済み'],['ghost','/','ゴーストモード'],['scan','/scan','スキャン操作'],['empty','/scan','応答のないスキャン'],['offline','/account','アカウント'],['offline','/logout','ログアウト確認'],['offline','/delete-account','削除確認']];
export default function Preview(){return <ScrollView contentContainerStyle={styles.page}><Text style={styles.title}>なわばり・デザイン確認</Text><Text style={styles.text}>本番の画面・ルーティング・状態管理をそのまま使っています。下のリンクは開始状態を設定し、そこから本番と同じ操作で進めます。</Text><Text style={styles.text}>実データには接続しません。登録後は「デザイン操作」から「確認メールを開いたことにする」を押し、ログインしてください。サンプルユーザーは designer@example.test / preview123 です。</Text>{entries.map(([name,path,label])=><Pressable key={label} accessibilityRole="button" onPress={()=>{scenario(name);window.location.assign(path);}} style={styles.link}><Text>{label} →</Text></Pressable>)}<View style={{height:32}}/><Text style={styles.text}>通信障害・GPS拒否・圏外移動・NFCキャンセル・次の操作の失敗は「デザイン操作」で変更できます。再読み込みしてもこのタブ内の状態を維持します。</Text></ScrollView>}
const styles=StyleSheet.create({page:{padding:24},title:{fontSize:24,fontWeight:'600',marginBottom:20},text:{fontSize:14,lineHeight:23,color:'#555',marginBottom:20},link:{padding:18,borderWidth:1,borderColor:'#ddd',borderRadius:10,marginBottom:10}});

